use crate::context::oauth::{entity::TokenPair, error::DomainError};

pub struct ExchangeTokenUseCase {}

impl ExchangeTokenUseCase {
    pub fn execute() -> Result<TokenPair, DomainError> {
        // 
        todo!("");
    }
}
